<%- include('partials/header') -%>

<h1>Home</h1>
<p><%= startingContent %></p>

<% posted.forEach(function(post) { %>
    <h1><%= post.title %></h1>
    <p><%= post.desc.substring(0, 100) + "..." %><a href="<%= "post/" + post.title %>">Read More</a></p>
    <!-- post/<post.title> -->
<% }) %>

<%- include('partials/footer') -%>